#include <stdio.h>

/**
 * main -entry point
 *
 * a code that print alphabet in lowercase using putchar
 *
 * Return: Always 0 (sucess)
 */

int main(void)
{
	int ch;

	for (ch = "a"; ch <= "z"; ch++;)
		putchar(ch);

Return: 0 ;
}
